#!/usr/bin/env ruby
require 'yaml'

payload = {
  'addons'                => [],
  'config_vars'           => {},
  'default_process_types' => { 'web' => 'bundle exec --verbose rackup config.ru -p $PORT' }
}.to_yaml

puts payload

# config_vars:
#  RACK_ENV: production
# default_process_types:
#  web: bundle exec rackup config.ru -p $PORT
