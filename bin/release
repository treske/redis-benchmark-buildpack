#!/usr/bin/env ruby
require 'yaml'

payload = {
  'addons'                => [],
  'config_vars'           => {},
  'default_process_types' => { 'web' => 'echo "12345" && sh boot.sh' }
}.to_yaml

puts payload
