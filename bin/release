#!/usr/bin/env ruby
require 'yaml'

payload = {
  'addons'                => [],
  'config_vars'           => {},
  'default_process_types' => { 'web' => 'sh /home/vcap/app/boot.sh' }
}.to_yaml

puts payload
